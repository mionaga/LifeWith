<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>投稿一覧</title>
</head>
<body>
    <h1>投稿一覧</h1>
    <ul>
        <% for(let post of posts){ %>
            <li><a href="/posts/<%= post._id %>">タイトル：<%= post.title %></a></li>
            <li>本文：<%= post.body %></li>
        <% } %>
    </ul>
</body>
</html>